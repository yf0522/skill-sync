{
  "name": "skill-sync",
  "version": "0.1.0",
  "description": "One canonical home for AI agent skills (Claude Code, Codex, Cursor, ...). Symlinks them into each tool so deleting a tool never loses your skills.",
  "type": "module",
  "bin": {
    "skill-sync": "bin/skill-sync.mjs"
  },
  "scripts": {
    "start": "node bin/skill-sync.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
